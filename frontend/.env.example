# Backend API base URL used by the frontend.
VITE_API_URL=http://127.0.0.1:5000/api

# Optional WebSocket endpoint for live dashboard updates.
# Leave unset to use reliable local state and worker-computed metrics.
VITE_REALTIME_URL=
